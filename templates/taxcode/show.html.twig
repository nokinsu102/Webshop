{% extends 'base.html.twig' %}

{% block title %}Taxcode{% endblock %}

{% block body %}
    <h1>Taxcode</h1>

    <table class="table">
        <tbody>
            <tr>
                <th>Id</th>
                <td>{{ taxcode.id }}</td>
            </tr>
            <tr>
                <th>Country</th>
                <td>{{ taxcode.country }}</td>
            </tr>
            <tr>
                <th>Value</th>
                <td>{{ taxcode.value }}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('taxcode_index') }}">back to list</a>

    <a href="{{ path('taxcode_edit', {'id': taxcode.id}) }}">edit</a>

    {{ include('taxcode/_delete_form.html.twig') }}
{% endblock %}
